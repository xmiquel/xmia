# Task 5 Report — Frontend: Accumulate candles and infinite scroll in Layout

**Status:** DONE

**Commits:**
- `e921554` feat: add infinite scroll with accumulated candle state in Layout

**Test summary:** 25 tests passed across 8 test files (all tests)

**Concerns:** None — all tests pass, TypeScript checks pass, and the implementation follows the brief exactly (refs-based closure prevention, merge-on-poll logic, and `onVisibleRangeChange` prop passing).
